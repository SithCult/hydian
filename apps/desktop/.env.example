# Artwork is hosted by default. Set / only when you have populated public/ yourself.
VITE_ASSET_BASE=https://tiles.hydian.org/
# Optional: use a local API instead of api.hydian.org (development only: copy to .env.development.local).
VITE_SERVER_URL=http://localhost:8080
